# Review package: 6a72a6c..541a03d

## Commits
541a03d feat(auth): add bcrypt dep, env config, register auth router/middleware

## Files changed
 .env.local              | 15 +++++++++++++++
 apps/backend/src/app.py |  6 ++++++
 requirements.txt        |  1 +
 3 files changed, 22 insertions(+)

## Diff
diff --git a/.env.local b/.env.local
new file mode 100644
index 0000000..5d04edf
--- /dev/null
+++ b/.env.local
@@ -0,0 +1,15 @@
+FQP_MODE=local_development
+FQP_AUTH_MODE=none
+FQP_DAILY_BUDGET=500
+FQP_THEME=red_black_tech
+FQP_BIND_HOST=127.0.0.1
+DATABASE_URL=postgresql://fqp:fqp_local_password@127.0.0.1:5432/fqp
+REDIS_URL=redis://127.0.0.1:6379/0
+OFFICIAL_SOURCE_ENABLED=true
+SPORTTERY_BASE_URL=https://webapi.sporttery.cn/gateway/jc/football/
+API_FOOTBALL_KEY=cb17f8badead2096569b89c5d3fae917
+
+# Authentication
+FQP_AUTH_MODE=none
+FQP_ADMIN_PASSWORD_HASH=$2b$12$odVPUwlVShrwyNAiS4i3W.Bf4aH4frp2yUAusG47VVZAibwz3TC6K
+FQP_SESSION_TTL=86400
diff --git a/apps/backend/src/app.py b/apps/backend/src/app.py
index 7a4d13e..a888667 100644
--- a/apps/backend/src/app.py
+++ b/apps/backend/src/app.py
@@ -18,20 +18,22 @@ from apps.backend.src.routers import (
     official,
     ops,
     pool,
     predictions,
     simulator,
     teams,
     tickets,
     ui,
     upsets,
 )
+from apps.backend.src.auth import AuthMiddleware
+from apps.backend.src.routers import auth_router
 
 
 class ApiV1AliasMiddleware:
     """Route /api/v1/* requests to the existing /api/* handlers."""
 
     def __init__(self, app: ASGIApp) -> None:
         self.app = app
 
     async def __call__(self, scope: Scope, receive: Receive, send: Send) -> None:
         if scope["type"] == "http":
@@ -45,20 +47,24 @@ class ApiV1AliasMiddleware:
 def create_app() -> FastAPI:
     app = FastAPI(
         title="FQP From Scratch API",
         description="足彩/竞彩足球量化研究与长期运行系统。仅用于数据分析、模拟和复盘，不提供售彩、代购、出票、收款。",
         version="1.2.0",
     )
     app.add_middleware(ApiV1AliasMiddleware)
     tickets.UPLOAD_ROOT.mkdir(parents=True, exist_ok=True)
     app.mount("/uploads", StaticFiles(directory=str(tickets.UPLOAD_ROOT)), name="uploads")
 
+    # Auth router must be registered before middleware so login/logout bypass checks
+    app.include_router(auth_router.router)
+    app.add_middleware(AuthMiddleware)
+
     app.include_router(health.router)
     app.include_router(official.router)
     app.include_router(teams.router)
     app.include_router(predictions.router)
     app.include_router(tickets.router)
     app.include_router(agents.router)
     app.include_router(enrichment.router)
     app.include_router(ops.router)
     app.include_router(backtests.router)
     app.include_router(pool.router)
diff --git a/requirements.txt b/requirements.txt
index 7cb4acc..f83d20c 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -8,20 +8,21 @@ scipy==1.18.0
 pandas==3.0.3
 scikit-learn==1.9.0
 pyyaml==6.0.3
 httpx==0.28.1
 beautifulsoup4==4.15.0
 playwright==1.61.0
 python-multipart==0.0.32
 jinja2==3.1.6
 reportlab==4.4.9
 apscheduler==3.11.3
+bcrypt==4.3.0
 redis==8.0.1
 alembic==1.18.5
 loguru==0.7.3
 
 # Analysis (Batch 5 — feature importance + SHAP explainability)
 xgboost==3.3.0
 shap==0.52.0
 # Python 3.14 compatibility pair required by SHAP's native runtime.
 numba==0.66.0
 llvmlite==0.48.0
