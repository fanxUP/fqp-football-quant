# 模块重组与注册表修复验收记录

## 本次调整

- `model_research_module` 只依赖 `official_data_core` 和 `multidim_feature_module`。
- `pool_lottery_module` 依赖 `official_data_core` 和 `model_research_module`，不再依赖竞彩推荐核心。
- `multidim_feature_module` 新增独立页面 `/feature-snapshots`。
- `data_health` 从 `official_data_core` 拆分到独立 `ops_admin` 运维模块。
- 模块加载器、面板加载器和开发文档统一使用 `final_*_registry.yaml`。

## 当前运行结构

```text
官方数据核心
  -> 多维赛前情报
  -> 模型研究与回测
  -> 推荐与资金核心
  -> 投注中心
```

传统足彩作为独立策略实验模块，不直接混入竞彩足球推荐流程；系统监控作为运维模块，只读取数据健康和任务状态。

## 验收证据

- 运行时模块：10 个，依赖缺失数 0。
- 运行时面板：14 个，面板引用模块缺失数 0。
- 多维特征面板：`/feature-snapshots`，分类为研究优化。
- 系统监控面板：`/data-health`，模块为 `ops_admin`。
- 前端全量测试：15 个文件、102 个测试通过。
- 注册表和侧边栏专项测试：30 个测试通过。
- 前端生产构建通过。

## 兼容边界

`configs/module_registry.yaml` 和 `configs/panel_registry.yaml` 保留作历史兼容参考，不再用于新增模块、依赖、页面或权限。
